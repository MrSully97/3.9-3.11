{
  "name": "3.9-3.11",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Connor Sullivan",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../2.16-2.17 && npm run build && cp -r dist ../3.9-3.11",
    "build:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
